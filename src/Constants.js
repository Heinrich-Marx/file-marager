const EXIT = ".exit"

const UP = "up"

const CD = "cd"

const LS = "ls"

const CAT = "cat"

const ADD = "add"

const RN = "rn"

const CP = "cp"

const MV = "mv"

const RM = "rm"

const OS_EOL = "os --EOL"

const OS_CPUS = "os --cpus"

const OS_HOMEDIR = "os --homedir"

const OS_USERNAME = "os --username"

const OS_ARCHITECTURE = "os --architecture"

const HASH = "hash"

const COMPRESS = "compress"

const DECOMPRESS = "decompress"

const COMMANDS = [
  EXIT,
  UP,
  LS,
  CD,
  CAT,
  ADD,
  RN,
  CP,
  MV,
  RM,
  OS_EOL,
  OS_CPUS,
  OS_HOMEDIR,
  OS_USERNAME,
  OS_ARCHITECTURE,
  HASH,
  COMPRESS,
  DECOMPRESS
]

export {EXIT,UP,CD,LS,CAT,ADD,RN,CP,MV,RM,OS_EOL,OS_CPUS,OS_HOMEDIR,OS_USERNAME, OS_ARCHITECTURE,HASH,COMPRESS, DECOMPRESS, COMMANDS}